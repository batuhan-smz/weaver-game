node_modules/
.env
.DS_Store
*.log
dist/
# Firebase config files contain secrets — never commit these
src/firebase-config.js
www/src/firebase-config.js
# Google Services JSON contains API keys — keep local only
android/app/google-services.json
